<section class="hero-copy" aria-labelledby="home-title">
  <div>
    <p class="hero-overline">Research · Code · Everyday</p>
    <h1 id="home-title">把世界<br />写成<span>笔记</span></h1>
  </div>
  <div class="hero-intro">
    <p>我记录正在学习的技术、实验里发生的问题，以及那些值得慢慢想清楚的日常。这里不是简历，是一份持续更新的个人档案。</p>
    <a href="/about/">进入我的世界 <span aria-hidden="true">→</span></a>
  </div>
  <slot />
</section>

<style>
  .hero-copy { min-width: 0; min-height: 670px; display: flex; flex-direction: column; justify-content: space-between; padding: 70px 52px 38px 0; border-right: 1px solid var(--line); }
  .hero-overline { margin: 0; color: var(--cyan); font: 400 11px 'DM Mono', monospace; letter-spacing: .12em; text-transform: uppercase; }
  h1 { margin: 34px 0 0; font: 600 clamp(58px, 8vw, 102px)/.9 'Noto Serif SC', serif; letter-spacing: 0; }
  h1 span { color: var(--coral); }
  .hero-intro { display: flex; justify-content: space-between; align-items: flex-end; gap: 28px; }
  .hero-intro p { max-width: 510px; margin: 36px 0 0; color: var(--muted); font-size: 14px; line-height: 1.75; }
  .hero-intro a { flex: 0 0 auto; display: inline-flex; gap: 9px; align-items: center; padding: 12px 15px; border-radius: 4px; background: var(--ink); color: var(--bg); font-size: 12px; }
  @media (max-width: 820px) { .hero-copy { min-height: auto; border-right: 0; border-bottom: 1px solid var(--line); padding: 52px 0 32px; gap: 44px; } h1 { font-size: 68px; } }
  @media (max-width: 520px) { h1 { font-size: 51px; } .hero-intro { align-items: flex-start; flex-direction: column; } }
</style>
